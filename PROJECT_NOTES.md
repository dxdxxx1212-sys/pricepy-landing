# Восток Прицеп — памятка проекта (для новой сессии)

Бренд: **Восток Прицеп**, слоган «Прицепы для дела» (раньше было «ПрайсПрицеп» / «ПрицепМаркет»).

## Реквизиты (РЕАЛЬНЫЕ, подтверждены пользователем)
- **ИП Зырин Валерий Владимирович**, ИНН **644154970400**, ОГРНИП **320774600488876**, г. Москва.
- Телефон **+7 (495) 487-38-38**.
- E-mail info@jefwipwero.online — ⚠️ пока плейсхолдер, нужен реальный ящик.

## Сервер / хостинг
- VDSina, **Москва**, Ubuntu 24.04, IP **45.150.39.174**, каталог сайта `/var/www/pricepy`.
- Автодеплой (обновлено 2026-08-26): **cron каждую минуту `git fetch + git reset --hard`** (раньше был `git pull`, он ЗАЛИПАЛ при «грязном» рабочем дереве — каталог отстал на 8 коммитов). Строки crontab:
  - `* * * * * cd /var/www/catalog && git fetch -q origin && git reset --hard -q origin/master >/dev/null 2>&1` (каталог, ветка **master**)
  - `* * * * * cd /var/www/pricepy && git fetch -q origin && git reset --hard -q origin/main >/dev/null 2>&1 && chown -R www-data:www-data /var/www/pricepy` (лендинги, ветка **main**, chown обязателен для записи leads.log)
- ⚠️ **НЕ редактировать файлы прямо на сервере** (`/var/www/*`) — `reset --hard` их затрёт. Все правки только через GitHub. `leads.log`/`leads-errors.log` untracked → не затираются.
- **Каталог** отдаётся из `/var/www/catalog` (git-клон `trailers-catalog`) на **каталог.восток-прицеп.рф** (HTTPS ✅) и catalog.jefwipwero.online.
- ⚠️ **На сервер по SSH захожу НЕ я** (не работаю с паролями) — команды выполняет пользователь (SSH или VNC-консоль VDSina).
- ⚠️ root-пароль сервера светился в чате — **сменить** (панель VDSina «Изменить пароль root»).

## Домены
| Домен | Статус |
|---|---|
| **jefwipwero.online** (Dynadot) | лендинг, HTTPS ✅ |
| **восток-прицеп.рф** (DNS на sprinthost, ns*.sprinthost.ru) | лендинг, HTTPS ✅. punycode `xn----ctbklixakchgm2d.xn--p1ai` |
| **подбор.восток-прицеп.рф** | отдаёт v2.html, **HTTPS ✅ РАБОТАЕТ** (сертификат выпущен, проверено 2026-08-13). punycode `xn--90af3acbk.xn----ctbklixakchgm2d.xn--p1ai` |

### ✅ ЗАКРЫТО (2026-08-13): HTTPS для подбор.восток-прицеп.рф РАБОТАЕТ. Ниже — история, как чинили.
- Причина провала certbot: у DNS-записей `подбор` и `www.подбор` есть **AAAA (IPv6) → 2a0a:2b42:11e:8039::** (не наш сервер, автодобавил sprinthost). Let's Encrypt идёт по IPv6 → 404 → отказ.
- **Фикс:** в панели DNS (sprinthost) **удалить обе AAAA-записи** (подбор + www.подбор). A-записи → 45.150.39.174 оставить.
- **Rate-limit LE:** было 5 неудач → повтор только **после 2026-08-13 00:16 UTC (~03:16 МСК)**.
- После удаления AAAA + сброса лимита, на сервере:
  `certbot --nginx -d xn--90af3acbk.xn----ctbklixakchgm2d.xn--p1ai --redirect`

### Скрипты деплоя (в репо pricepy-landing, отдаются через GitHub Pages)
- `domain-vostok.sh` — подключил восток-прицеп.рф к лендингу (сделано).
- `subdomain-podbor.sh` — поднимает подбор.восток-прицеп.рф с главной = v2.html (nginx-блок создан; ждём HTTPS).
- Запуск на сервере: `curl -fsSL https://dxdxxx1212-sys.github.io/pricepy-landing/<script>.sh | bash`

## Сайты
- **Лендинг** — репо `pricepy-landing`, файл `index.html` (тёмный hero, квиз, отзывы, FAQ, документы). Одностраничник, инлайн CSS+JS.
- **v2.html** — упрощённая вариация (квиз-воронка): тёмный hero «−40% ОТ РЫНКА» + лого, квиз (каналы: Телефон/Telegram/WhatsApp/**МАКС**), блок «ведущие производители» (честные бренды-чипы, НЕ чужие корпо-логотипы), «Популярные модели», отзывы, футер с реквизитами+документами. Доступен на всех доменах как /v2.html и на подбор.восток-прицеп.рф как главная.
- `reviews/1-6.jpg` — реальные фото покупателей (тексты — временные). `images/1-32.jpg` — фото товаров (временные, с конкурента).
- **Каталог** — репо `trailers-catalog` (ветка **master**), ~251 прицеп, домен **каталог.восток-прицеп.рф** (+ GitHub Pages зеркало), бренд Восток Прицеп, фильтры (бренд/тип/оси/грузоп./цена/поиск), deep-links `?p=slug-id`, кнопка «Скопировать ссылку» (кириллический домен).
  - ⚠️ **`build_data.py` — DEPRECATED, НЕ пересобирать!** Он тянет из устаревшего `merged_source.json` (теряет ~32 товара, добавленных напрямую), возвращает баги данных (грузоп>масса, тонны как кг, оси) и **перенумеровывает id** (ломает ссылки/подборки). **Источник правды = `window.PRODUCTS` в index.html** (данные вычищены: грузоп=полная−снаряжённая, тонны→кг, оси из названия, тип, дедуп, описания перегенерированы).
  - Таблица подборок (name/бренд/тип/оси/грузоп/масса/длина/ширина/цена/ссылка) генерится из index.html скриптом → `/Users/a1111/Downloads/Claude/каталог-товары-ссылки.xlsx` (+csv). Перегенерировать при каждом изменении товаров.

## Приём заявок (РАБОТАЕТ)
Форма → `POST /api/lead` (PHP, `api/lead.php`) → сохраняет в leads.log + шлёт в **Cloudflare Worker** → Telegram.
- Worker: `throbbing-union-7326pricepy-leads.dxdxxx1212.workers.dev` (секреты BOT_TOKEN, CHAT_ID).
- Сейчас лиды идут в **группу Telegram, chat_id `-5388770296`** («Pricep group»), бот **@prycepyrfbot** в группе. (CHAT_ID в Cloudflare = -5388770296.)
- Почему воркер: с РФ-сервера api.telegram.org недоступен.
- ⚠️ Токен бота светился в чате — по-хорошему перевыпустить (@BotFather /revoke) и обновить в Cloudflare Variables.
- ✅ ФИКС ДОСТАВКИ (2026-08-18): раньше при пачках заявок часть терялась (лимит Telegram ~20/мин на группу + блокирующая отправка обрывалась по таймауту lead.php). Воркер переписан: отправка в фоне через `ctx.waitUntil` + до 4 повторов с учётом `retry_after`. Проверено пачкой из 5 — дошли все. Код воркера живёт ТОЛЬКО в Cloudflare (в репо его нет). Формат сообщения и секреты не менялись.
- Важно: **заявки НЕ теряются на сервере в любом случае** — все пишутся в `/var/www/pricepy/leads.log` (полная история с 17.06.2026). Терялось только уведомление в Telegram.
- Анализ leads.log (18.08.2026): почти всё — тесты (имена типа «щупецш», фейк-номера «4567654345»). Единственный правдоподобно реальный лид: **10.08 Сергей, МАКС, +7 953 447-18-02** (не переотправлен — решить, слать ли в TG).

## Пиксель VK (Top.Mail.Ru) — ТЕПЕРЬ ПО ДОМЕНАМ (обновлено 2026-08-13)
Пиксель зависит от домена. Раскладка (проверено в браузере через `window.VK_PIXEL`):
| Домен / файл | pageView id | goal при заявке |
|---|---|---|
| **восток-прицеп.рф** (index.html) | **3787092** | **Lead2** |
| **jefwipwero.online** (index.html) | **3787094** | **Lead3** |
| **подбор.восток-прицеп.рф** и `/v2.html` везде (v2.html) | **3787094** | **Lead3** |

- В `index.html` (один файл на восток-прицеп.рф + jefwipwero.online) выбор через JS по `location.hostname`:
  `window.VK_PIXEL = /(^|\.)(восток-прицеп\.рф|xn----ctbklixakchgm2d\.xn--p1ai)$/.test(location.hostname) ? {id:"3787092",goal:"Lead2"} : {id:"3787094",goal:"Lead3"}` — используется и в pageView, и в `reachGoal` (sendLead).
- `v2.html` — статично 3787094 / Lead3.
- `<noscript>`-счётчик в index.html статичен на 3787094 (домен без JS определить нельзя) — редкий кейс.
- Цель `reachGoal` срабатывает ТОЛЬКО при отправке заявки (детектор показывает на загрузке из-за статического скана — норма).
- Старый единый пиксель **3785703 / goal Leed_js** — БОЛЬШЕ НЕ ИСПОЛЬЗУЕТСЯ (заменён).
- Пользователю: привязать пиксели к рекламному кабинету VK Ads, выбрать нужное событие Lead для оптимизации.

## TODO до запуска рекламы
1. ~~HTTPS подбор.восток-прицеп.рф~~ — ✅ СДЕЛАНО (сертификат выпущен, 2026-08-13).
2. Реальный **e-mail** (info@… сейчас плейсхолдер).
3. Заменить **фото прицепов** на свои (сейчас временные с конкурента) — лендинг + каталог.
4. Сменить **root-пароль** сервера; перевыпустить **токен бота**.
5. Уведомление в **РКН** об обработке ПДн.
6. ~~Рекламные заявления «−40%», «2 500+ клиентов», «без риска» и т.п.~~ — ✅ ВЫЧИЩЕНО под модерацию Директа на ОБОИХ лендингах (index.html + v2.html), 2026-08-18. Убрано: −40%/«дешевле розницы», 2500+/9800 клиентов, «сэкономили 50 000», «без риска», фейк «Продан N дней», фейк-скидка, блок «лидеры отраслей»+partners.webp. Смягчено: гарантия→«по талону завода 12–36 мес», возврат→«по закону», рассрочка→«уточняйте», ПТС→ЭПТС. Галки согласия сняты с предвыбора (+валидация в квизе index).

## Осталось перед запуском Директа (нужны данные пользователя)
- **Точный адрес** вместо «г. Москва» (модерация не любит «общий диапазон») — в футер + юр-доки, оба лендинга.
- **Реальный e-mail** info@восток-прицеп.рф (ящик должен принимать) — см. п.2.
- **Свои фото прицепов** без чужих водяных знаков — см. п.3.
- **Рассрочка/кредит** — подтвердить, есть ли реально; если нет — убрать упоминания совсем.
- **Отзывы** — тексты выдуманы, по возможности заменить реальными.
- Директ льём на **подбор** (v2.html): цель Метрики `lead` рабочая; стратегия — старт на кликах, через 1–2 недели на конверсии по цели `lead`.
- Аккаунт Директа = венгерская Kft **AL-EF 93 AUDIT** (рег. 01-09-268577); строка «Рекламодатель» добавлена в футер v2.html, на index НЕ ставили.

## Как работать (предпочтения)
- Не переделывать/не перепроверять уже сделанное; при неожиданном отрицательном результате проверки — сначала заподозрить кривую проверку, а не «чинить» рабочую фичу. Без длинных циклов ожидания.
